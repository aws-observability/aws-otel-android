<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <provider
                android:name="software.amazon.opentelemetry.android.agent.AgentProvider"
                android:authorities="${applicationId}.aws-rum-auto-instrumentation-initializer"
                android:enabled="true"
                android:exported="false">
        </provider>
    </application>
</manifest>